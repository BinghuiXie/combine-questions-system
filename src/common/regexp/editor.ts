export const fillBlankRules = /[\?\？]/g;
export const keyRenderClass = /([a-zA-Z]+__)([a-zA-Z]+)(-[a-zA-Z]+)/;

// const children = Array.from((typesetElement as HTMLDivElement).children)
//         const nodeKeyClass = children.map(child => {
//             return (child.getAttribute('class') as string)
//             .replace(keyRenderClass, (substring: string, ...args: any[]) => {
//                 return args[1];
//             })
//         });
//         console.log(nodeKeyClass);
